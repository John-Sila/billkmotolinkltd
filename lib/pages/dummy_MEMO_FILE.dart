import 'package:flutter/material.dart';

class CreateMemoDUMMY extends StatelessWidget {
  const CreateMemoDUMMY({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Create a Memo",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
